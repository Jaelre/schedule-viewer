# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Next.js
.next/
out/
build
dist

# Production
build/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
# NOTE: Environment files are now tracked and encrypted via git-crypt (see .gitattributes)
# Keep .env.example files untracked as they are templates

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Rust / Cargo
worker/target/
worker/debug/
**/*.rs.bk
*.pdb
worker/build/
worker/.wrangler/
Cargo.lock
# NOTE: worker/.dev.vars and worker/wrangler-local.toml are tracked and encrypted via git-crypt

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# API samples (exploration scripts)
.api-samples/

# Sensitive information and private files
docs/private/
public/config/doctor-names.json
public/config/full-name-overrides.json
public/config/shift-colors.json
public/config/shift-styling.config.json
*.xlsx
*.xls
*.csv
**/sensitive/
**/confidential/
# NOTE: Sensitive files are now tracked and encrypted via git-crypt (see .gitattributes)
# Only truly generated/temporary files should be ignored here
claudedocs/

# Python
# NOTE: scripts/update_siamo_numeri.py is tracked and encrypted via git-crypt
__pycache__/
*.pyc
*.pyo

# Local AI tooling artifacts
.claude/
.wrangler/

# git-crypt
# DO NOT ignore .git-crypt/ directory (needed for git-crypt to work)
# Only ignore git-crypt key file if accidentally placed in repo root
/git-crypt-key
